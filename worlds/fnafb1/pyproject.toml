[build-system]
requires = ["setuptools>=69"]
build-backend = "setuptools.build_meta"

[project]
name = "worlds.fnafb1"
requires-python = ">=3.13"

classifiers = ["Private :: Do Not Upload"]

[tool.setuptools.packages.find]
where = ["src"]
include = ["worlds.fnafb1"]
namespaces = true